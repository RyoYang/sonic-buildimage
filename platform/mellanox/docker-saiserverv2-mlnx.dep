# DPKG FRK

DPATH := $($(DOCKER_SAISERVERV2_MLNX)_PATH)
DEP_FILES := $(SONIC_COMMON_FILES_LIST) $(PLATFORM_PATH)/docker-saiserverv2-mlnx.mk $(PLATFORM_PATH)/docker-saiserverv2-mlnx.dep
DEP_FILES += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES += $(shell git ls-files -- $(DPATH))

$(DOCKER_SAISERVERV2_MLNX)_CACHE_MODE := GIT_CONTENT_SHA
$(DOCKER_SAISERVERV2_MLNX)_DEP_FLAGS := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SAISERVERV2_MLNX)_DEP_FILES := $(DEP_FILES)
